{
  "_note": "Garmin MCP disabled — Cloudflare blocks garminconnect auth. Using Supabase MCP instead (data synced nightly by garmin_sync.py).",
  "supabase": {
    "command": "npx",
    "args": ["-y", "@supabase/mcp-server-supabase@latest"],
    "env": {
      "SUPABASE_ACCESS_TOKEN": "${SUPABASE_ACCESS_TOKEN}"
    }
  }
}
